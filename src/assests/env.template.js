(function (window) {
  window.__env = window.__env || {};
  window.__env.movieServiceUrl = "${MOVIE_SERVICE_URL}";
  window.__env.reviewServiceUrl = "${REVIEW_SERVICE_URL}";
  window.__env.enableTracing = true;
})(this);

